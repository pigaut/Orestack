--- # ===== Phase 1 =====
type: depleted
growth: 30s
block: oak_log

hologram:
  update: 3
  offset.y: 1
  line: '&7Growing: {generator_timer}'

--- # ===== Phase 2 =====
type: ripe
health: 5   # If you set the health idle will be true by default
hit-cooldown: 7   # The cooldown before on-hit is triggered.
structure: oak_tree_1
decorative-blocks: [oak_leaves]   # These blocks do not trigger functions (on-break, on-click, on-hit)

hologram:
  offset: { x: 1, y: 1.25 }
  lines:
    - 'Required Tool: Axe'
    - '[Click to Chop]'
    - '{generator_health_bar:simple}'


# Triggered when a player left-clicks a block
on-hit:
  - requireAxe   # Global function from functions/tools/axe.yml
  - do:
      - damageTool
      - damageGenerator #1-10 # You can specify an amount or leave it blank to use the tool damage in config.yml
      - sendHologram &7Chop! offsetY=1 rangeX=0.7 rangeY=0.4 rangeZ=0.7 duration=10

# Triggered when a generator runs out of health (You can remove health with damageGenerator)
on-destroy:
  do: dropItemAtBlock oak_log

--- # ===== Phase 3 =====
type: ripe
health: 5
hit-cooldown: 7
structure: oak_tree_2
decorative-blocks: [oak_leaves]

hologram:
  offset: { x: 1, y: 1.25 }
  lines:
    - 'Required Tool: Axe'
    - '[Click to Chop]'
    - '{generator_health_bar:simple}'

on-hit:
  - requireAxe
  - do:
      - damageTool
      - damageGenerator
      - sendHologram &7Chop! offsetY=1 rangeX=0.7 rangeY=0.4 rangeZ=0.7 duration=10

on-destroy:
  do: dropItemAtBlock oak_log

--- # ===== Phase 4 =====
type: ripe
health: 5
hit-cooldown: 7
structure: oak_tree_3
decorative-blocks: [oak_leaves]

hologram:
  offset: { x: 1, y: 1.25 }
  lines:
    - 'Required Tool: Axe'
    - '[Click to Chop]'
    - '{generator_health_bar:simple}'

on-hit:
  - requireAxe
  - do:
      - damageTool
      - damageGenerator
      - sendHologram &7Chop! offsetY=1 rangeX=0.7 rangeY=0.4 rangeZ=0.7 duration=10

on-destroy:
  do: dropItemAtBlock oak_log

--- # ===== Phase 5 =====
type: regrown
health: 5
hit-cooldown: 7
structure: oak_tree_4
decorative-blocks: [oak_leaves]

hologram:
  offset: { x: 1, y: 1.25 }
  lines:
    - 'Required Tool: Axe'
    - '[Click to Chop]'
    - '{generator_health_bar:simple}'

on-hit:
  - requireAxe
  - do:
      - damageTool
      - damageGenerator
      - sendHologram &7Chop! offsetY=1 rangeX=0.7 rangeY=0.4 rangeZ=0.7 duration=10

on-destroy:
  do: dropItemAtBlock oak_log